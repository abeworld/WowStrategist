# Prepared corpus

Normalized, matchup-split evidence package for later strategy synthesis.

Start here:

- `03_READINESS_REPORT.md` — counts and READY_WITH_KNOWN_GAPS
- `normalized_matches.jsonl` — every valid 2v2 record
- `matchups/` — one JSONL per canonical matchup
- `matchup_stats.json` — facts only (no strategy)
- `00_INPUT_INVENTORY.md` — every original artifact

Original extraction files under `analysis/<batch>/` were not modified.
